class AuthService {
    static login = (username, password) => {
        //todo login api
    }
    static register = (name, username, password) => {
        // todo register api
    }
}

export default AuthService;